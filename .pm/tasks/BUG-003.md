---
id: BUG-003
title: Restore static collision wireframes in connected debug modes
track: BUG
createdAt: 2026-07-10T06:42:37.4776210Z
modifiedAt: 2026-07-10T06:55:08.1363610Z
---

In a connected client, F7 DebugOnly renders the network player capsule, spawn markers, and safe-zone circle but omits every static collision wireframe, including the floor, perimeter walls, and interior boxes. F5 and F6 still render world solids, and F8 still renders collision solids.

Root cause: DebugSceneBuilder appends Box3D static-world lines only through localPlayer.CollisionWorld. Connected clients intentionally have localPlayer == null; their equivalent MapStaticCollisionWorld is already owned by ClientMovementPrediction, but it is not exposed to the debug rendering path.

Implement the smallest ownership-safe correction: let connected debug rendering consume the existing prediction collision world without creating a duplicate physics world or changing prediction, protocol, authority, map schema, or solid-rendering behavior. Preserve offline F6/F7 behavior and prediction-world disposal ownership.

Acceptance:
- Connected F6 includes static collision wireframes over world solids.
- Connected F7 includes floor, wall, and box wireframes alongside player/spawn/safe-zone primitives.
- Offline F6/F7 behavior remains unchanged.
- F5 and F8 routing remains unchanged.
- Automated coverage proves the connected debug builder receives static collision geometry and prevents regression.
- Human validation covers F5-F8 in a connected client.

## Notes

- 2026-07-10 06:51 UTC - Implemented the ownership-safe connected debug rendering fix: NetworkClientRuntime exposes an assembly-internal read-only reference to the MapStaticCollisionWorld owned by ClientMovementPrediction; DebugSceneBuilder accepts an optional borrowed static collision world, falls back to LocalPlayerController.CollisionWorld offline, and appends static Box3D lines once independently of offline player primitives; SdlApplication supplies the prediction world for connected rendering. Added a connected-style native debug-builder regression test with localPlayer null, a network snapshot, and an explicitly supplied collision world; it covers static collision, network player, spawn, and safe-zone primitives and verifies the borrowed world is not disposed. Validation passed: `dotnet build Royale.slnx -m:1 --no-restore` (0 warnings/errors), `dotnet test Royale.slnx -m:1 --no-restore` (all 748 tests passed), and PM `validate_project` (valid, no issues). The existing content-and-rendering wiki already documents F6/F7 collision debug geometry, so no wiki edit was needed. OTLP-enabled dedicated server started on graybox:7777 and accepted a connected client; visual F5-F8 owner validation requested.
- 2026-07-10 06:55 UTC - Project-owner connected-client visual validation passed on 2026-07-10: the regression is fixed across the requested F5-F8 render-mode check. The validation client was closed by the project owner, and the OTLP-enabled dedicated server was then stopped cleanly.