# Royale Skills

Repo-scoped Codex skills for the Royale project.

Codex scans `.agents/skills` from the current working directory up to the repository root. Each skill is a directory containing a required `SKILL.md` file.

Use the root `AGENTS.md` to keep always-on rules small and route Codex to the relevant skill.
